apiVersion: 1
policies:
  - orgId: 1
    receiver: slack
    group_by:
      - alertname
      - instance_id
    routes:
      - receiver: slack
        matchers:
          - alertname = "EC2 High CPU"
